export interface ResponseMedicine {
    medicineId: number,
    code: number,
    nameMedicine: string,
    coveragePos: boolean
}

export interface Medicine {
    medicineId: number,
    code: number,
    nameMedicine: string,
    coveragePos: boolean
}